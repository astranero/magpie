# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/
.nyc_output/
test-results/

# Production
build/
dist/

# Misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.openrouter-key

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vite
.vite/

# TypeScript
*.tsbuildinfo

# Opencode (local session/cache state only — skills are tracked)
.opencode/*
!.opencode/skill/
# ── Quarantine ──
# `.trash/` holds files removed from the project but kept locally until you are
# sure they are not needed. Never committed.
.trash/

# ── Never commit ──
# Extension signing keys and packed builds. A .pem here is a private key: anyone
# who has it can sign a package Chrome accepts as an authentic update.
*.pem
*.crx
*.p12
*.key

# Build output
dist/
build/
*.tsbuildinfo

# Local env / secrets
.env
.env.*
!.env.example

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*~
